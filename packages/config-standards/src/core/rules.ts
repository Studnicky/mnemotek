export const REQUIRED_GITIGNORE_LINES: readonly string[] = Object.freeze([
  'node_modules/',
  'dist/',
  '*.tsbuildinfo'
])

export const REQUIRED_PACKAGE_FIELDS: readonly string[] = Object.freeze([
  'license',
  'engines',
  'repository'
])
